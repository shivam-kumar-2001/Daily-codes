#include<stdio.h>
int main()
{
	int i=0,k=0,arr[100],n;
	scanf("%d",&n);
	printf("%d\n%d\n",0,1);
	arr[0]=0;
	arr[1]=1;
    for(i=0;i<n;i++)
    {
    	arr[i+2]=arr[i]+arr[i+1];
    	printf("%d\n",arr[i+2]);
	}
	return 0;
}
